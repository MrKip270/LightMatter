// LightMatter backend — entry point
//
// This file will eventually start a web server that:
//   1. Listens for requests from the frontend (e.g. "what's viewable at lat/lon?")
//   2. Calls the external APIs (clouds, aurora, satellites, weather alerts)
//   3. Sends the combined result back to the browser
//
// We haven't installed a server library yet (that's the next checkpoint),
// so for now this is just a placeholder so the folder exists in Git.

console.log("LightMatter backend — not wired up yet");
